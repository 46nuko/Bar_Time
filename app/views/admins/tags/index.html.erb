<% @tags.each do |tag| %>
  <div>
    <p><%= tag.name %></p>
  </div>
<% end %>

<%= form_with model: [:admins, @tag] do |f| %>
  <h4>タグ名</h4>
  <%= f.text_field :name %>
  <%= f.submit '投稿' %>
<% end %>