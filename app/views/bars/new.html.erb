<h1>投稿フォーム</h1>
<%= form_with model: @bar do |f| %>
  <h4>bar名</h4><%= f.text_field :name %>
<% end %>
<%= form_with model: @comments do |f| %>
  <h4>タイトル</h4>
  <%= f.text_field :title %>
  <h4>説明</h4>
  <%= f.text_area :content %>
  <%= f.submit '投稿' %>
<% end %>